% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eta_tuning_bimodal.R
\name{eta_tuning_bimodal}
\alias{eta_tuning_bimodal}
\title{Select \eqn{\eta} for cross-modality contrastive spectral clustering by 3CA or s3CA}
\usage{
eta_tuning_bimodal(
  Xt,
  Yt,
  Xa,
  Ya,
  eta_range = seq(0.5, 10, 0.5),
  plot = TRUE,
  sparse_ctst = NULL,
  sparse_trt = NULL,
  sparse_ctrl = NULL,
  reference = c("target", "ancillary"),
  num_comps = 1,
  km_cluster = 2,
  ckm_cluster = 2,
  nstart = 25
)
}
\arguments{
\item{Xt}{data frame for one data type in target group, samples in rows, variables in columns. To tune based on cross correlation matrices, each feature should be centered and standardized.}

\item{Yt}{data frame for another data type in target group, samples in rows, variables in columns.}

\item{Xa}{data frame for one data type in ancillary group, samples in rows, variables in columns.}

\item{Ya}{data frame for another data type in ancillary group, samples in rows, variables in columns.}

\item{eta_range}{a vector containing candidate eta range to be tuned from.}

\item{plot}{a logical quantity indicating whether plot the ARI(nuisance) and silhouette score.}

\item{sparse_ctst}{parameter controlling the sparsity of s3CA, if NULL, use 3CA, else use s3CA. If vector, refer sumabss in PMD.cv from package PMA, if single number, refer sumabs in PMD in package PMA.}

\item{sparse_trt}{parameter controlling the sparsity of the target-only (sparse) cross covariance analysis used to define the reference labels when \code{reference = "target"}. NULL means non-sparse.}

\item{sparse_ctrl}{parameter controlling the sparsity of the ancillary-only (sparse) cross covariance analysis used to define the reference labels when \code{reference = "ancillary"}. NULL means non-sparse.}

\item{reference}{which non-contrastive analysis defines the reference (nuisance) arrangement of the target samples. \code{"target"} (default) mimics \code{eta_tuning_general} and clusters the target samples on the target-only leading component pair. \code{"ancillary"} clusters the target samples after projecting them on the ancillary-only leading component pair, which targets the nuisance direction directly.}

\item{num_comps}{number of leading contrastive singular pairs used to build the score matrix for clustering. Default is 1, i.e. the top pair, giving two score columns (\eqn{X_t\hat u_1}, \eqn{Y_t\hat v_1}).}

\item{km_cluster}{number of nuisance clusters defined from kmeans clustering on the reference score pair.}

\item{ckm_cluster}{number of interesting clusters defined from kmeans clustering on the contrastive score pair.}

\item{nstart}{number of random starts passed to \code{\link[stats]{kmeans}}.}
}
\value{
A list containing the selected value of \eqn{\eta} \code{eta_opt}, the candidate grid \code{eta_range}, the vector of ARI(nuisance) \code{nuisance_ARI}, the vector of silhouette scores \code{silhouette}, the reference labels \code{nuisance_labels}, and the ggplot object \code{plot}.
}
\description{
Bimodal counterpart of \code{\link{eta_tuning_general}}. The rationale is the
same as in the single-modality algorithm: a feasible \eqn{\eta} should produce
a contrastive arrangement of the target samples that (i) disagrees with the
reference (nuisance) arrangement obtained without contrast, measured by a small
ARI, and (ii) is itself well separated, measured by a large average silhouette
score. The selected value maximizes \eqn{\mbox{Sil}(\eta) - \mbox{ARI}(\eta)}.
}
\examples{
library(ConSpec)
data(intro)
eta_tuning_bimodal(Xt = intro$cCCA$Xt, Yt = intro$cCCA$Yt,
                   Xa = intro$cCCA$Xa, Ya = intro$cCCA$Ya,
                   eta_range = seq(0.1, 5, 0.1), plot = TRUE)
}
