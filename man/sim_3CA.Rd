% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_3CA.R
\name{sim_3CA}
\alias{sim_3CA}
\title{Simulate Contrastive Cross Covariance Analysis Data}
\usage{
sim_3CA(
  nt = 200,
  na = 200,
  p = 100,
  q = 100,
  mu = 1,
  theta = 400,
  eta_true = 2.5,
  repetition = 50,
  eta = seq(0.1, 6, 0.1),
  km_cluster = 2,
  ckm_cluster = 2
)
}
\arguments{
\item{nt}{Integer. Number of target samples (default 200).}

\item{na}{Integer. Number of ancillary samples (default 200).}

\item{p}{Integer. Number of X features (default 100).}

\item{q}{Integer. Number of Y features (default 100).}

\item{mu}{Numeric. Signal strength parameter (default 1.0).}

\item{theta}{Numeric. Noise variance parameter (default 400).}

\item{eta_true}{Numeric. True signal-to-noise ratio (default 2.5).}

\item{repetition}{Integer. Number of simulation repetitions (default 50).}

\item{eta}{Contrast parameter rule. A numeric vector of length > 1 is the candidate grid
handed to \code{\link{eta_tuning_bimodal}}, which is the default. A single number is used
directly. \code{"ratio"} instead uses the spectral-scale estimator
\eqn{\lambda_1(S_t)/\lambda_1(S_a)}, which is recorded in the \code{Eta_Ratio} column of
the output either way.}

\item{km_cluster}{Integer. Number of reference (nuisance) clusters used by the tuning algorithm.}

\item{ckm_cluster}{Integer. Number of contrastive clusters used by the tuning algorithm.}
}
\value{
A data frame with columns: Iteration, ARI_Target, ARI_Contrastive, Eta_Est, Eta_Ratio
}
\description{
Simulates data for a contrastive cross covariance analysis scenario. Computes clustering performance using standard CCA and
contrastive CCA approaches.
}
\examples{
# See vignettes/ContrastiveSpectralClustering.Rmd for example usage.
}
