% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_clust.R
\name{spec_clust}
\alias{spec_clust}
\title{Cluster samples using ConSpec component scores}
\usage{
spec_clust(
  score_mat,
  k = NULL,
  group = NULL,
  components = NULL,
  nstart = 25,
  iter.max = 100,
  seed = NULL,
  ...
)
}
\arguments{
\item{score_mat}{A numeric matrix or data frame containing component scores,
with samples in rows and components in columns.}

\item{k}{Number of clusters. If \code{NULL} and \code{group} is supplied, \code{k} is set
to the number of unique non-missing reference labels.}

\item{group}{Optional vector of reference group labels. Must have one value
per row of \code{score_mat}.}

\item{components}{Integer vector specifying the score dimensions used for
clustering. By default, all columns of \code{score_mat} are used.}

\item{nstart}{Number of random initializations passed to
\code{\link[stats:kmeans]{stats::kmeans()}}. Default is 25.}

\item{iter.max}{Maximum number of k-means iterations.}

\item{seed}{Optional random seed for reproducibility.}

\item{...}{Additional arguments passed to \code{\link[stats:kmeans]{stats::kmeans()}}.}
}
\value{
A list containing:
\describe{
\item{cluster}{Integer vector of cluster assignments.}
\item{ari}{Adjusted Rand index, or \code{NULL} if \code{group} is not supplied.}
\item{kmeans}{The fitted object returned by \code{\link[stats:kmeans]{stats::kmeans()}}.}
\item{scores}{The score matrix used for clustering.}
\item{components}{The component indices used for clustering.}
}
}
\description{
Performs k-means clustering on component scores obtained from a ConSpec
analysis, such as contrastive PCA or contrastive CCA. If reference labels
are supplied, the adjusted Rand index (ARI) is also calculated.
}
\examples{
set.seed(1)
scores <- matrix(rnorm(100), nrow = 20)

# Unsupervised clustering
fit <- spec_clust(scores, k = 2)
fit$cluster

# Clustering evaluation using known labels
group <- rep(c("A", "B"), each = 10)
fit <- spec_clust(scores, k = 2, group = group)
fit$ari
}
